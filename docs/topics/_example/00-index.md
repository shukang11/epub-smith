---
nav:
  hidden: true
---

# 示例 Topic（勿作正式规格）

> **用途：** 可复制骨架。新建 topic 时复制本目录为 `docs/topics/<kebab-case>/`，改标题与各节正文，并在 `architecture.md` §4 登记。不进预览导航。

## 范围

（本 topic 解决什么问题、不解决什么；读者是谁。）

## 行为契约

（正常路径下的输入、输出、状态变化；可链到 API / UI 原型。）

## 失败与边界

（错误码、空值、权限、并发、超时等边界；谁负责重试或补偿。）

## 相关链接

（其他 topic、原型页、外部依赖文档——只链不复述。）
