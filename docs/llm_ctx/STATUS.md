# 项目状态快照

> 维护人可在每次功能合并或发布前后更新本文件。

## 基本信息

- 当前版本：`0.2.0`
- 最后更新：`2026-02-15`
- 下一里程碑：`v0.3.0`

## 近期完成

- 命令行子命令优化：重构为清晰的分层子命令结构
  - `convert`：TXT转EPUB的主要命令
  - `preview outline`：打印章节大纲
  - `preview dry-run`：预览详细章节结构
  - `snapshot save`：保存章节结构快照
  - `snapshot load`：从快照生成EPUB
  - `template export`：导出样式模板
- 支持多文件输入，可同时处理多个TXT文件
- 支持从标准输入（stdin）读取内容，使用 `-` 作为输入参数
- 优化了文件处理逻辑，提高了多文件处理效率
- 改进了命令行参数处理，支持更灵活的输入方式
- 新增 `doctor` 诊断命令
  - 输出章节数据异常（重复号、倒序）统计与样本
  - 输出规则风险提示（如“张”单位误判、中文数字范围不足）
  - 输出可执行的下一步建议命令
- 默认阅读样式改为方案 C
  - 保留首行两字符缩进
  - 使用中等段落间距
  - 默认保留段内换行（`white-space: pre-line`）
- 默认章节规则强修正
  - 默认禁用“张”单位，降低正文误判为章节的风险
  - 默认支持 `百/千/万/零/〇/两` 等中文大数字章节
  - 默认章节单位调整为 `章/节/卷/回`，源文本错号仍仅告警不自动修复
- 新增默认封面生成（`--cover auto`）
  - 离线 SVG 模板 → resvg 栅格化 PNG，无网络/模型依赖
  - 兼容 Kindle/部分阅读器：补 cover.xhtml 封面页并置于 spine 首位
  - `template export` 附带导出可编辑的 `cover.svg` 模板
  - 修复 OPF manifest 中 nav.xhtml 重复声明问题
- 增强 `template export` 的预览页
  - 示例章节扩至 6 个，覆盖多样排版场景
  - 新增目录 + 翻页 + 键盘切换交互
  - 顶部示例封面块
- 页面结构规范化
  - 章节/目录页包 `.container` 内容容器，消除死代码
  - 封面页改 class 化（`.cover-page`/`.cover-image`），替换内联样式

## 当前重点

- 规则验证：在加载规则时验证正则表达式的有效性
- 交互式规则生成：添加向导式规则生成功能

## 风险与阻塞

- 无明显阻塞事项

## 下一步计划

- 实现并行处理章节功能，提高大文件处理速度
- 添加更多输出格式支持
- 实现插件系统设计
